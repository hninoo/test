import { DoubleScrollbarDirective } from './double-scrollbar.directive';

describe('DoubleScrollbarDirective', () => {
  it('should create an instance', () => {
    const directive = new DoubleScrollbarDirective();
    expect(directive).toBeTruthy();
  });
});
